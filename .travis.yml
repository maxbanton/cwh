language: php

php:
  - '5.5'
  - '5.6'
  - '7.0'
  - hhvm
  - nightly

before_script:
  - composer self-update
  - composer install --no-interaction --dev -o

script:
  - vendor/bin/phpunit --coverage-textphpunit
  - vendor/bin/phpcs --standard=PSR2 src/
